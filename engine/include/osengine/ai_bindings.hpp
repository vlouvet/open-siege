#ifndef DTS_VIEWER_AI_BINDINGS_HPP
#define DTS_VIEWER_AI_BINDINGS_HPP

// Spec 18/02 — linker anchor for the AI::* DefineEngineFunction surface.
// Same pattern as anchorHudBindings() / anchorPlayerClass().

namespace dts_viewer {
void anchorAIBindings();
}

#endif // DTS_VIEWER_AI_BINDINGS_HPP
