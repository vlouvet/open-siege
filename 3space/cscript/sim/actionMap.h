#ifndef _ACTIONMAP_SHIM_H_
#define _ACTIONMAP_SHIM_H_
// Open Siege spec 15/02b — empty shim for sim/actionMap.h.
// simSet.cpp includes this header but never references any ActionMap
// symbols from it. The include is dead weight for our build.
#endif
